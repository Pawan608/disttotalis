---
// Accepts: text, delay (optional)
const { text, delay = 0 } = Astro.props;
---

<span
  class="reveal inline-block"
  role="presentation"
  aria-label={text}
  data-delay={delay}
>
  {text}
</span>
